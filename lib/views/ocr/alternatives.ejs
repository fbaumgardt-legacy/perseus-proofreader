<% if (wordType(alt)!="default") { for (k in alt.children) { if (k!='last') { var del = alt.children[k] %>
<li <% for (a in del.attributes) { %><%- a+"=\""+del.attributes[a]+"\" "%><% } %>><a href="#"><%= del.text %></a></li>
<% }} %>
<li class="divider"></li>
<% } %>